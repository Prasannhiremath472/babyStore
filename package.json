{
  "name": "mybaby-store",
  "version": "2.0.0",
  "description": "My Baby — The New Born Baby Shop",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "install": "cd backend && npm install",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
